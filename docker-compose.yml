services:
  hexcate-backend:
    image: ghcr.io/riskirills66/hexcate:init
    container_name: hexcate-backend
    user: "0:0"
    init: true
    env_file:
      - .env
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    volumes:
      - ${HEXDIAL_CONFIG_DIR}:/root/.config/hexdial
      - ${HEXDIAL_DATA_DIR}:/root/.local/share/hexdial
      - ${HEXDIAL_CACHE_DIR}:/root/.cache
    restart: unless-stopped
